# Centr Invest ML

Система интеллектуального анализа личных финансов — backend-сервис для автоматической классификации транзакций и прогнозирования расходов. Проект разработан для хакатона и включает полный цикл: от обучения ML-моделей до REST API для инференса.

## Содержание

- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [ML-модули (third-party)](#ml-модули-third-party)
- [Быстрый старт](#быстрый-старт)
- [API-эндпоинты](#api-эндпоинты)
- [Запуск в Docker](#запуск-в-docker)
- [Разработка](#разработка)

## Возможности

- 🔐 **Аутентификация** — регистрация, логин, JWT-токены
- 📊 **Импорт транзакций** — загрузка CSV с банковскими операциями
- 🤖 **Автоклассификация** — категоризация транзакций с помощью CatBoost
- 📈 **Аналитика** — агрегации по категориям и временные ряды
- 🔔 **Уведомления** — real-time WebSocket уведомления
- ⏰ **Фоновые задачи** — автоматическая классификация через APScheduler

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| Web-фреймворк | FastAPI + Uvicorn |
| ORM | SQLAlchemy 2.0 (async) |
| База данных | PostgreSQL + asyncpg |
| Миграции | Alembic |
| DI-контейнер | Dishka |
| ML | CatBoost, scikit-learn, pandas |
| Аутентификация | JWT (PyJWT) + bcrypt |
| Планировщик | APScheduler |
| Менеджер зависимостей | uv |

## Структура проекта

```
centr_invest_ml/
├── app/                          # Основное приложение FastAPI
│   ├── __main__.py               # Точка входа (uvicorn)
│   ├── core/
│   │   └── app.py                # Фабрика приложения, lifespan, DI
│   ├── models/                   # SQLAlchemy модели
│   │   ├── user.py               # Пользователь (email, password, role)
│   │   ├── transaction.py        # Транзакция (date, withdrawal, deposit, balance, category)
│   │   └── enums.py              # UserRole, TransactionCategory
│   ├── routes/                   # API роуты
│   │   ├── auth.py               # /auth — регистрация, логин, /me
│   │   ├── transactions.py       # /transactions — импорт, список, аналитика
│   │   ├── ml.py                 # /ml — метрики модели
│   │   ├── users.py              # /users — управление пользователями
│   │   └── ws.py                 # /ws/notifications — WebSocket
│   ├── schemas/                  # Pydantic схемы
│   ├── services/                 # Бизнес-логика
│   │   ├── auth/                 # Регистрация, логин
│   │   ├── transactions/         # Импорт, классификация, аналитика
│   │   ├── users/                # CRUD пользователей
│   │   └── providers/            # Провайдеры (ML классификатор, JWT, bcrypt)
│   ├── deps/                     # DI провайдеры и зависимости
│   ├── settings/                 # Конфигурация (pydantic-settings)
│   └── alembic/                  # Миграции БД
│       └── versions/
├── third-party/                  # ML-модули (workspace packages)
│   ├── category-classifier/      # Классификатор категорий транзакций
│   └── forecasting/              # Прогнозирование расходов
├── docker/
│   ├── entrypoint.sh
│   ├── infra.compose.yml
│   ├── init.sql
│   └── nginx.conf
├── tests/
├── compose.yml
├── Dockerfile
├── pyproject.toml
└── alembic.ini
```

## ML-модули (third-party)

Проект использует два ML-модуля, оформленных как workspace packages:

### 📦 [category-classifier](third-party/category-classifier/README.md)

Классификатор категорий банковских транзакций на основе CatBoost.

- **Вход:** дата, сумма списания/пополнения, баланс
- **Выход:** категория (Food, Misc, Rent, Salary, Shopping, Transport) + вероятности
- **Метод:** feature engineering + CatBoost с балансировкой классов
- **Метрики:** Macro F1, Balanced Accuracy, per-class precision/recall

### 📦 [forecasting](third-party/forecasting/README.md)

Модель прогнозирования месячных расходов пользователя.

- **Вход:** история транзакций за несколько месяцев
- **Выход:** прогноз суммарных расходов на следующий месяц
- **Метод:** месячные агрегации + лаги + Ridge/RandomForest
- **Метрики:** MAE, RMSE, MAPE

## Быстрый старт

### Требования

- Python 3.13+
- PostgreSQL 15+
- [uv](https://docs.astral.sh/uv/) — менеджер пакетов

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/your-org/centr-invest-ml.git
cd centr-invest-ml

# Установка зависимостей
uv sync

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env: DB_*, APP_JWT_SECRET
```

### Переменные окружения

| Переменная | Описание |
|------------|----------|
| `DB_USERNAME` | Имя пользователя PostgreSQL |
| `DB_PASSWORD` | Пароль PostgreSQL |
| `DB_DATABASE` | Имя базы данных |
| `DB_HOST` | Хост БД |
| `APP_JWT_SECRET` | Секретный ключ для JWT |

### Запуск

```bash
# Применение миграций
uv run alembic upgrade head

# Запуск сервера
uv run python -m app
```

Сервис будет доступен по адресу `http://localhost:8000/api`.

Swagger UI: `http://localhost:8000/api/docs`

## API-эндпоинты

### Аутентификация

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/auth/register` | Регистрация нового пользователя |
| `POST` | `/auth/login` | Авторизация, получение JWT |
| `GET` | `/auth/me` | Текущий пользователь |

### Транзакции

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/transactions/import` | Импорт CSV с транзакциями |
| `GET` | `/transactions` | Список транзакций (пагинация) |
| `GET` | `/transactions/analytics` | Аналитика по категориям и временным рядам |

### ML

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/ml/report` | Метрики обученной модели классификатора |

### WebSocket

| Путь | Описание |
|------|----------|
| `/ws/notifications?token=<JWT>` | Real-time уведомления |

### Health

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/health` | Статус сервиса и фоновых задач |

## Запуск в Docker

### Docker Compose

```bash
# Запуск backend + PostgreSQL
docker compose up --build
```

Сервис будет доступен на `http://localhost:8001/api`.

### Только backend (с внешней БД)

```bash
docker build -t centr-invest-ml .
docker run --rm -p 8000:8000 \
  -e DB_USERNAME=postgres \
  -e DB_PASSWORD=secret \
  -e DB_DATABASE=fin-matcher \
  -e DB_HOST=host.docker.internal \
  -e APP_JWT_SECRET=your-secret \
  centr-invest-ml
```

## Разработка

### Обучение моделей

```bash
# Классификатор категорий
uv run python -m category_classifier.train \
  --data ./third-party/category-classifier/resources/data/ci_data.csv \
  --model ./third-party/category-classifier/resources/models/transaction_classifier.joblib

# Прогнозирование расходов
uv run python -m forecasting.train \
  --data ./third-party/forecasting/resources/data/ci_data.csv \
  --model ./third-party/forecasting/resources/models/expense_forecast.joblib
```

### Проверка кода

```bash
# Линтер
uv run ruff check .

# Типы
uv run mypy app
```

### Миграции

```bash
# Создание новой миграции
uv run alembic revision --autogenerate -m "описание"

# Применение миграций
uv run alembic upgrade head
```

## Архитектура

### Dependency Injection

Проект использует [Dishka](https://github.com/reagento/dishka) для DI. Все провайдеры регистрируются в `app/deps/__init__.py`:

- **AppProvider** — настройки приложения
- **DbConnectionProvider** — сессии SQLAlchemy
- **AuthServicesProvider** — интеракторы аутентификации
- **TransactionServicesProvider** — сервисы транзакций

### Фоновые задачи

`TransactionBackgroundClassifier` запускается каждую минуту через APScheduler и классифицирует транзакции без категории. После классификации отправляет WebSocket уведомление пользователю.

### ML Pipeline

```
CSV Upload → Parse → Transaction records → Background Classifier
                                               ↓
                        CategoryClassifierService (CatBoost)
                                               ↓
                        Update Transaction.category → Notification
```

## Лицензия

MIT
